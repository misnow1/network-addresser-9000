__pycache__/
*.pyc
db.sqlite3
.env
.env.deployment
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
staticfiles/
.DS_Store
# Local testing scratchpad — drained into issues/ROADMAP, never committed
TESTING-NOTES.md

# Transient review notes from the plan-cycle chain — the durable record is the
# plan's "Review response" table and the fix commits
REVIEW-*.md

# claude scratch space — except the committed project skills
.claude/*
!.claude/skills/

# Production data for reference
prod/
